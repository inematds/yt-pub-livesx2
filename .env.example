# <raiz>/.env — so o que e GLOBAL do hub. Segredos de canal ficam no config_dir de cada canal.
# Bot dedicado de avisos (nao reutilizar token de outro processo que faca getUpdates: da 409)
TELEGRAM_NOTIFY_BOT_TOKEN=
# Senha do painel (vazio = sem senha; painel so na rede local)
HUB_DASHBOARD_PASSWORD=
